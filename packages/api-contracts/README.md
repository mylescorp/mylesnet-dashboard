# @mylesnet/api-contracts

Versioned, OpenAPI-derived API contracts (per `docs/technology-stack.md` Monorepo
Shape). Skeletons only — no runtime code yet. The API layer must not silently
diverge from the UI; shared contracts live here so the type-level boundary holds.
