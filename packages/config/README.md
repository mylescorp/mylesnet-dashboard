# @mylesnet/config

Shared configuration (tsconfig/base, env contracts, design-token references).
Skeletons only — no runtime code yet. Env identity is documented in `.env.example`
per the Master Final Document Control.
