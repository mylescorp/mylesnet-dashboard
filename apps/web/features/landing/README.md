# Landing

The public route layer is `app/(public)/`. Its landing implementation, content,
components, and assets are progressively consolidated here; this boundary owns
marketing, legal, resources, and conversion UI.
